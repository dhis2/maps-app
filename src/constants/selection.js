// Values for state.ui.selectionFilter - controls which features are shown
// (in both the data table and on the map) based on their selection state.
export const SELECTION_FILTER_SELECTED = 'selected'
export const SELECTION_FILTER_NOT_SELECTED = 'not-selected'
