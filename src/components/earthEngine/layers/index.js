import elevation from './elevation.js'

const earthEngineLayers = [elevation]

/*
export const getEarthEngineLayer = (id) =>
    earthEngineLayers.find((l) => l.layerId === id)
*/

export default earthEngineLayers
